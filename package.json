{
  "name": "oil-intelligence-ai",
  "private": true,
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "web:dev": "npm run dev --workspace apps/web",
    "web:lint": "npm run lint --workspace apps/web",
    "web:test": "npm run test --workspace apps/web"
  }
}
